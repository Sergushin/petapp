import Layout from "@/components/Layout";
import { Center } from "@chakra-ui/react";

const User = () => {
    return (
        <Layout>
            User
        </Layout>
    );
}

export default User;